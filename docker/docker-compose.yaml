services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: estate_backend
    restart: always
    env_file:
      - ../backend/.env
    volumes:
      - ../backend:/backend
      - /backend/node_modules
    ports:
      - "4000:4000"
      - "9229:9229"
